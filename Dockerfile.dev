# 开发环境Dockerfile
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装开发依赖
RUN apk add --no-cache git

# 复制package文件
COPY package*.json ./

# 使用国内镜像源安装依赖
RUN npm config set registry https://registry.npmmirror.com/ && \
    npm ci

# 暴露端口
EXPOSE 3000

# 创建非root用户
RUN addgroup -g 1001 -S frostyflow && \
    adduser -S frostyflow -u 1001

# 设置权限
RUN chown -R frostyflow:frostyflow /app

# 切换到非root用户
USER frostyflow

# 启动开发服务器
CMD ["npm", "start"]